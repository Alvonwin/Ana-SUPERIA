.manual-page {
  padding: 40px;
  max-width: 1400px;
  margin: 0 auto;
}

.manual-header {
  text-align: center;
  margin-bottom: 40px;
}

.manual-header h1 {
  font-size: 2.8em;
  margin-bottom: 10px;
  background: linear-gradient(135deg, #f39c12, #e67e22);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.manual-header p {
  font-size: 1.2em;
  color: #94a3b8;
}

/* Table of Contents */
.toc {
  background: #1e293b;
  padding: 25px;
  border-radius: 12px;
  margin-bottom: 40px;
}

.toc h3 {
  color: #f39c12;
  margin-bottom: 15px;
  font-size: 1.3em;
}

.toc-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 8px;
}

.toc-item {
  background: #334155;
  padding: 10px 15px;
  border-radius: 6px;
  cursor: pointer;
  transition: all 0.2s ease;
  font-size: 0.9em;
}

.toc-item:hover {
  background: #475569;
  transform: translateX(5px);
}

/* Manual Content */
.manual-content {
  display: flex;
  flex-direction: column;
  gap: 30px;
}

/* Manual Section */
.manual-section {
  background: #1e293b;
  border-radius: 12px;
  overflow: hidden;
  border: 1px solid #334155;
}

/* Section Header - Clickable */
.section-header-clickable {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 25px;
  cursor: pointer;
  transition: background 0.2s ease;
  user-select: none;
}

.section-header-clickable:hover {
  background: rgba(243, 156, 18, 0.1);
}

.section-title-group {
  display: flex;
  align-items: center;
  gap: 15px;
}

.section-icon {
  color: #f39c12;
  font-size: 1.5em;
}

.section-title-group h2 {
  font-size: 1.6em;
  color: #f1f5f9;
  margin: 0;
}

.expand-icon {
  color: #64748b;
  transition: transform 0.3s ease;
}

.expand-icon.expanded {
  transform: rotate(180deg);
}

/* Section Body */
.section-body {
  padding: 0 25px 25px;
  animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(-10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* Step Cards */
.step-cards {
  display: grid;
  gap: 15px;
  margin-top: 15px;
}

.step-card {
  display: flex;
  align-items: flex-start;
  gap: 15px;
  background: #334155;
  padding: 20px;
  border-radius: 10px;
  border-left: 4px solid #f39c12;
}

.step-number {
  background: #f39c12;
  color: #1e293b;
  width: 32px;
  height: 32px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  flex-shrink: 0;
}

.step-content h4 {
  color: #f39c12;
  margin-bottom: 8px;
  font-size: 1.1em;
}

.step-content p {
  color: #94a3b8;
  line-height: 1.6;
}

.step-content code {
  background: #1e293b;
  padding: 2px 8px;
  border-radius: 4px;
  color: #2ecc71;
  font-size: 0.9em;
}

/* Info Box */
.info-box {
  background: rgba(59, 130, 246, 0.1);
  border: 1px solid #3b82f6;
  padding: 15px 20px;
  border-radius: 8px;
  margin-top: 15px;
  color: #93c5fd;
}

/* Tip Box */
.tip-box {
  background: rgba(34, 197, 94, 0.1);
  border: 1px solid #22c55e;
  padding: 15px 20px;
  border-radius: 8px;
  margin-top: 15px;
  display: flex;
  align-items: flex-start;
  gap: 12px;
  color: #86efac;
}

.tip-box svg {
  flex-shrink: 0;
  margin-top: 2px;
}

/* Warning Box */
.warning-box {
  background: rgba(245, 158, 11, 0.1);
  border: 1px solid #f59e0b;
  padding: 15px 20px;
  border-radius: 8px;
  margin-top: 15px;
  display: flex;
  align-items: flex-start;
  gap: 12px;
  color: #fcd34d;
}

.warning-box svg {
  flex-shrink: 0;
  margin-top: 2px;
}

/* Support Box */
.support-box {
  background: rgba(139, 92, 246, 0.1);
  border: 1px solid #8b5cf6;
  padding: 20px;
  border-radius: 8px;
  margin-top: 15px;
  text-align: center;
}

.support-box p {
  color: #c4b5fd;
  margin: 5px 0;
}

.support-box strong {
  color: #a78bfa;
}

/* Feature Grid */
.feature-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 20px;
  margin-top: 15px;
}

.feature-card {
  background: #334155;
  padding: 20px;
  border-radius: 10px;
  border-top: 3px solid #3b82f6;
}

.feature-card h4 {
  color: #3b82f6;
  margin-bottom: 10px;
  font-size: 1.1em;
}

.feature-card p {
  color: #94a3b8;
  line-height: 1.6;
}

.feature-card ul {
  margin-top: 10px;
  padding-left: 20px;
  color: #94a3b8;
}

.feature-card li {
  margin: 5px 0;
}

/* LLM Grid */
.llms-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 20px;
  margin-top: 15px;
}

.llm-detailed-card {
  background: #334155;
  padding: 20px;
  border-radius: 10px;
  border: 1px solid #475569;
}

.llm-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 15px;
}

.llm-header h4 {
  color: #f1f5f9;
  font-size: 1.2em;
}

.llm-badge {
  padding: 4px 12px;
  border-radius: 20px;
  font-size: 0.75em;
  font-weight: 600;
}

.llm-badge.code {
  background: #3b82f6;
  color: white;
}

.llm-badge.chat {
  background: #22c55e;
  color: white;
}

.llm-badge.vision {
  background: #8b5cf6;
  color: white;
}

.llm-badge.math {
  background: #f59e0b;
  color: #1e293b;
}

.llm-specs {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.spec-row {
  display: flex;
  justify-content: space-between;
  font-size: 0.9em;
}

.spec-label {
  color: #64748b;
}

.spec-value {
  color: #f1f5f9;
}

/* Routing Table */
.routing-table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 15px;
  background: #334155;
  border-radius: 8px;
  overflow: hidden;
}

.routing-table th,
.routing-table td {
  padding: 12px 15px;
  text-align: left;
  border-bottom: 1px solid #475569;
}

.routing-table th {
  background: #1e293b;
  color: #f39c12;
  font-weight: 600;
}

.routing-table td {
  color: #e2e8f0;
}

.routing-table tr:last-child td {
  border-bottom: none;
}

.routing-table tr:hover td {
  background: rgba(243, 156, 18, 0.05);
}

/* API Table */
.api-table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 15px;
  background: #334155;
  border-radius: 8px;
  overflow: hidden;
}

.api-table th,
.api-table td {
  padding: 12px 15px;
  text-align: left;
  border-bottom: 1px solid #475569;
  font-size: 0.9em;
}

.api-table th {
  background: #1e293b;
  color: #3b82f6;
  font-weight: 600;
}

.api-table td {
  color: #e2e8f0;
}

.api-table code {
  background: #1e293b;
  padding: 3px 8px;
  border-radius: 4px;
  color: #2ecc71;
  font-size: 0.85em;
}

.api-table tr:hover td {
  background: rgba(59, 130, 246, 0.05);
}

/* Shortcuts Grid */
.shortcuts-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 15px;
  margin-top: 15px;
}

.shortcut-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #334155;
  padding: 15px;
  border-radius: 8px;
}

.shortcut-item span {
  color: #94a3b8;
}

kbd {
  background: linear-gradient(180deg, #475569, #334155);
  border: 1px solid #64748b;
  border-radius: 6px;
  padding: 6px 12px;
  font-family: inherit;
  font-size: 0.85em;
  color: #f1f5f9;
  box-shadow: 0 2px 0 #1e293b;
}

/* Architecture Section */
.architecture-diagram {
  background: #0f172a;
  padding: 20px;
  border-radius: 8px;
  margin-top: 15px;
  font-family: 'Consolas', 'Monaco', monospace;
  font-size: 0.85em;
  line-height: 1.5;
  overflow-x: auto;
  white-space: pre;
  color: #94a3b8;
}

.folder-structure {
  background: #0f172a;
  padding: 20px;
  border-radius: 8px;
  margin-top: 15px;
  font-family: 'Consolas', 'Monaco', monospace;
  font-size: 0.85em;
  line-height: 1.6;
  overflow-x: auto;
  white-space: pre;
  color: #94a3b8;
}

/* Troubleshooting */
.troubleshoot-grid {
  display: grid;
  gap: 15px;
  margin-top: 15px;
}

.troubleshoot-item {
  background: #334155;
  padding: 20px;
  border-radius: 10px;
  border-left: 4px solid #ef4444;
}

.troubleshoot-item h4 {
  color: #ef4444;
  margin-bottom: 10px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.troubleshoot-item p {
  color: #94a3b8;
  line-height: 1.6;
}

.troubleshoot-item code {
  background: #1e293b;
  padding: 2px 8px;
  border-radius: 4px;
  color: #2ecc71;
}

.solution {
  color: #22c55e !important;
  margin-top: 8px;
}

/* Memory Section Extras */
.memory-features {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 15px;
  margin-top: 15px;
}

.memory-feature {
  background: #334155;
  padding: 18px;
  border-radius: 10px;
  text-align: center;
}

.memory-feature h4 {
  color: #8b5cf6;
  margin-bottom: 8px;
}

.memory-feature p {
  color: #94a3b8;
  font-size: 0.9em;
}

/* Settings Grid */
.settings-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 15px;
  margin-top: 15px;
}

.setting-item {
  background: #334155;
  padding: 18px;
  border-radius: 10px;
}

.setting-item h4 {
  color: #f59e0b;
  margin-bottom: 8px;
  font-size: 1em;
}

.setting-item p {
  color: #94a3b8;
  font-size: 0.9em;
  line-height: 1.5;
}

/* Responsive */
@media (max-width: 768px) {
  .manual-page {
    padding: 20px;
  }

  .manual-header h1 {
    font-size: 2em;
  }

  .section-header-clickable {
    padding: 15px;
  }

  .section-title-group h2 {
    font-size: 1.3em;
  }

  .toc-grid {
    grid-template-columns: 1fr;
  }

  .feature-grid,
  .llms-grid,
  .shortcuts-grid {
    grid-template-columns: 1fr;
  }

  .routing-table,
  .api-table {
    font-size: 0.8em;
  }
}

/* Animations */
.manual-section {
  transition: box-shadow 0.3s ease;
}

.manual-section:hover {
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
}

/* Section-specific colors */
.section-chat .section-icon { color: #3b82f6; }
.section-coding .section-icon { color: #22c55e; }
.section-memory .section-icon { color: #8b5cf6; }
.section-dashboard .section-icon { color: #f59e0b; }
.section-comfyui .section-icon { color: #ec4899; }
.section-n8n .section-icon { color: #14b8a6; }
.section-settings .section-icon { color: #6366f1; }
.section-logs .section-icon { color: #64748b; }
.section-llm .section-icon { color: #06b6d4; }
.section-architecture .section-icon { color: #a855f7; }
.section-api .section-icon { color: #ef4444; }
.section-shortcuts .section-icon { color: #84cc16; }
.section-troubleshoot .section-icon { color: #f97316; }

/* Print styles */
@media print {
  .manual-page {
    padding: 10px;
    max-width: none;
  }

  .section-header-clickable {
    background: #f0f0f0 !important;
    color: #000 !important;
  }

  .manual-section {
    break-inside: avoid;
    border: 1px solid #ccc;
  }

  .toc {
    display: none;
  }
}

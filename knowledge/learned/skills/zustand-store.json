{
  "category": "zustand-store",
  "version": "1.0.0",
  "generatedBy": "Claude (Opus 4.5)",
  "description": "Zustand lightweight state management patterns",
  "skills": [
    {"id": "zus01", "type": "setup", "name": "Install Zustand", "description": "Add package", "pattern": "npm install zustand", "example": "Minimal state lib"},
    {"id": "zus02", "type": "store", "name": "Create Store", "description": "Define store", "pattern": "const useStore = create((set) => ({ count: 0, increment: () => set(state => ({ count: state.count + 1 })) }))", "example": "Basic store"},
    {"id": "zus03", "type": "store", "name": "Use Store", "description": "Access state", "pattern": "const count = useStore(state => state.count)", "example": "Select state"},
    {"id": "zus04", "type": "store", "name": "Use Actions", "description": "Call actions", "pattern": "const increment = useStore(state => state.increment); increment()", "example": "Trigger action"},
    {"id": "zus05", "type": "store", "name": "Get Full Store", "description": "All state", "pattern": "const { count, increment } = useStore()", "example": "Destructure store"},
    {"id": "zus06", "type": "update", "name": "Set State", "description": "Update state", "pattern": "set({ count: 10 }) or set(state => ({ count: state.count + 1 }))", "example": "Partial update"},
    {"id": "zus07", "type": "update", "name": "Replace State", "description": "Full replace", "pattern": "set(newState, true) // second arg replaces", "example": "Replace all"},
    {"id": "zus08", "type": "update", "name": "Get State", "description": "Read outside React", "pattern": "const state = useStore.getState()", "example": "Non-React access"},
    {"id": "zus09", "type": "update", "name": "Set Outside React", "description": "Update outside", "pattern": "useStore.setState({ count: 5 })", "example": "External update"},
    {"id": "zus10", "type": "subscribe", "name": "Subscribe", "description": "Listen changes", "pattern": "const unsub = useStore.subscribe((state) => console.log(state.count))", "example": "State listener"},
    {"id": "zus11", "type": "subscribe", "name": "Subscribe Selector", "description": "Partial subscribe", "pattern": "useStore.subscribe((state) => state.count, (count) => console.log(count))", "example": "Select subscription"},
    {"id": "zus12", "type": "middleware", "name": "Persist Middleware", "description": "Local storage", "pattern": "create(persist((set) => ({ ... }), { name: 'store-key' }))", "example": "Persist state"},
    {"id": "zus13", "type": "middleware", "name": "Immer Middleware", "description": "Mutable updates", "pattern": "create(immer((set) => ({ items: [], addItem: (item) => set(state => { state.items.push(item) }) })))", "example": "Direct mutation"},
    {"id": "zus14", "type": "middleware", "name": "DevTools Middleware", "description": "Redux DevTools", "pattern": "create(devtools((set) => ({ ... })))", "example": "Debug store"},
    {"id": "zus15", "type": "middleware", "name": "Combine Middleware", "description": "Multiple middleware", "pattern": "create(devtools(persist(immer((set) => ({ ... })))))", "example": "Stack middleware"},
    {"id": "zus16", "type": "typescript", "name": "TypeScript Store", "description": "Typed store", "pattern": "interface Store { count: number; increment: () => void }; const useStore = create<Store>()((set) => ({ ... }))", "example": "Type-safe"},
    {"id": "zus17", "type": "typescript", "name": "Typed Set", "description": "Typed setter", "pattern": "set: (partial: Store | Partial<Store> | ((state: Store) => Store | Partial<Store>)) => void", "example": "Set signature"},
    {"id": "zus18", "type": "async", "name": "Async Actions", "description": "Async operations", "pattern": "fetchData: async () => { const data = await api.get(); set({ data }) }", "example": "API calls"},
    {"id": "zus19", "type": "async", "name": "Loading State", "description": "Track loading", "pattern": "{ loading: false, fetchData: async () => { set({ loading: true }); ...; set({ loading: false, data }) } }", "example": "Loading flag"},
    {"id": "zus20", "type": "selector", "name": "Shallow Equality", "description": "Prevent rerenders", "pattern": "import { shallow } from 'zustand/shallow'; useStore(state => ({ a: state.a, b: state.b }), shallow)", "example": "Object select"},
    {"id": "zus21", "type": "selector", "name": "Computed Values", "description": "Derived state", "pattern": "const total = useStore(state => state.items.reduce((sum, i) => sum + i.price, 0))", "example": "Calculate in selector"},
    {"id": "zus22", "type": "slice", "name": "Store Slices", "description": "Modular state", "pattern": "const createUserSlice = (set) => ({ user: null, setUser: (user) => set({ user }) })", "example": "Split store"},
    {"id": "zus23", "type": "slice", "name": "Combine Slices", "description": "Merge slices", "pattern": "const useStore = create((...a) => ({ ...createUserSlice(...a), ...createCartSlice(...a) }))", "example": "Composed store"},
    {"id": "zus24", "type": "context", "name": "Context Store", "description": "Per-component", "pattern": "const StoreContext = createContext(); const useStore = () => useContext(StoreContext)", "example": "Scoped store"},
    {"id": "zus25", "type": "reset", "name": "Reset Store", "description": "Reset to initial", "pattern": "const initialState = { count: 0 }; reset: () => set(initialState)", "example": "Clear state"},
    {"id": "zus26", "type": "equality", "name": "Custom Equality", "description": "Compare function", "pattern": "useStore(selector, (a, b) => a.id === b.id)", "example": "Custom compare"},
    {"id": "zus27", "type": "transient", "name": "Transient Updates", "description": "No rerender", "pattern": "useStore.setState({ temp: value }, false, 'transient')", "example": "Silent update"},
    {"id": "zus28", "type": "vanilla", "name": "Vanilla Store", "description": "Non-React", "pattern": "import { createStore } from 'zustand/vanilla'; const store = createStore((set) => ({ ... }))", "example": "Framework agnostic"},
    {"id": "zus29", "type": "ssr", "name": "SSR Hydration", "description": "Server render", "pattern": "const useStore = create<Store>()(persist(..., { skipHydration: true }))", "example": "Next.js SSR"},
    {"id": "zus30", "type": "testing", "name": "Test Store", "description": "Unit testing", "pattern": "const initialState = useStore.getState(); afterEach(() => useStore.setState(initialState))", "example": "Reset between tests"}
  ]
}

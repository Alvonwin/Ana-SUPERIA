{
  "category": "ky-http-client",
  "version": "1.0.0",
  "generatedBy": "Claude (Opus 4.5)",
  "description": "Ky HTTP client fetch wrapper patterns",
  "skills": [
    {"id": "ky01", "type": "setup", "name": "Install Ky", "description": "Add package", "pattern": "npm install ky", "example": "Fetch wrapper"},
    {"id": "ky02", "type": "basic", "name": "GET Request", "description": "Fetch data", "pattern": "import ky from 'ky'; const data = await ky.get('https://api.example.com/users').json()", "example": "Simple GET"},
    {"id": "ky03", "type": "basic", "name": "POST Request", "description": "Send data", "pattern": "await ky.post('https://api.example.com/users', { json: { name: 'John' } }).json()", "example": "Create resource"},
    {"id": "ky04", "type": "basic", "name": "PUT Request", "description": "Update data", "pattern": "await ky.put('https://api.example.com/users/1', { json: { name: 'Jane' } }).json()", "example": "Full update"},
    {"id": "ky05", "type": "basic", "name": "PATCH Request", "description": "Partial update", "pattern": "await ky.patch('https://api.example.com/users/1', { json: { name: 'Jane' } }).json()", "example": "Partial update"},
    {"id": "ky06", "type": "basic", "name": "DELETE Request", "description": "Remove data", "pattern": "await ky.delete('https://api.example.com/users/1')", "example": "Delete resource"},
    {"id": "ky07", "type": "response", "name": "JSON Response", "description": "Parse JSON", "pattern": "const data = await ky('https://api.example.com/data').json()", "example": "Auto parse"},
    {"id": "ky08", "type": "response", "name": "Text Response", "description": "Get text", "pattern": "const text = await ky('https://api.example.com/text').text()", "example": "Text body"},
    {"id": "ky09", "type": "response", "name": "Blob Response", "description": "Get binary", "pattern": "const blob = await ky('https://api.example.com/file').blob()", "example": "Binary data"},
    {"id": "ky10", "type": "config", "name": "Search Params", "description": "Query string", "pattern": "ky.get('https://api.example.com/search', { searchParams: { q: 'term', page: 1 } })", "example": "URL params"},
    {"id": "ky11", "type": "config", "name": "Headers", "description": "Request headers", "pattern": "ky.get('https://api.example.com/me', { headers: { Authorization: 'Bearer token' } })", "example": "Auth header"},
    {"id": "ky12", "type": "config", "name": "Timeout", "description": "Request timeout", "pattern": "ky.get('https://api.example.com/data', { timeout: 5000 })", "example": "5 second timeout"},
    {"id": "ky13", "type": "config", "name": "JSON Body", "description": "Send JSON", "pattern": "ky.post(url, { json: { name: 'John', email: 'john@email.com' } })", "example": "JSON payload"},
    {"id": "ky14", "type": "instance", "name": "Create Instance", "description": "Custom ky", "pattern": "const api = ky.create({ prefixUrl: 'https://api.example.com', timeout: 10000, headers: { 'X-API-Key': 'key' } })", "example": "Configured instance"},
    {"id": "ky15", "type": "instance", "name": "Extend Instance", "description": "Extend config", "pattern": "const authApi = api.extend({ headers: { Authorization: 'Bearer token' } })", "example": "Add to instance"},
    {"id": "ky16", "type": "hooks", "name": "beforeRequest", "description": "Request hook", "pattern": "ky.create({ hooks: { beforeRequest: [(request) => { request.headers.set('X-Custom', 'value') }] } })", "example": "Modify request"},
    {"id": "ky17", "type": "hooks", "name": "afterResponse", "description": "Response hook", "pattern": "hooks: { afterResponse: [(request, options, response) => { console.log(response.status) }] }", "example": "Log response"},
    {"id": "ky18", "type": "hooks", "name": "beforeRetry", "description": "Retry hook", "pattern": "hooks: { beforeRetry: [({ request, retryCount }) => { console.log(`Retry ${retryCount}`) }] }", "example": "Track retries"},
    {"id": "ky19", "type": "retry", "name": "Retry Config", "description": "Auto retry", "pattern": "ky.get(url, { retry: { limit: 3, methods: ['get'], statusCodes: [408, 500, 502, 503, 504] } })", "example": "Retry on error"},
    {"id": "ky20", "type": "retry", "name": "Disable Retry", "description": "No retry", "pattern": "ky.get(url, { retry: 0 })", "example": "Single attempt"},
    {"id": "ky21", "type": "error", "name": "HTTPError", "description": "HTTP errors", "pattern": "try { await ky.get(url) } catch (error) { if (error instanceof ky.HTTPError) { const body = await error.response.json() } }", "example": "Handle 4xx/5xx"},
    {"id": "ky22", "type": "error", "name": "TimeoutError", "description": "Timeout error", "pattern": "try { await ky.get(url) } catch (error) { if (error instanceof ky.TimeoutError) { console.log('Request timed out') } }", "example": "Handle timeout"},
    {"id": "ky23", "type": "abort", "name": "Cancel Request", "description": "Abort request", "pattern": "const controller = new AbortController(); ky.get(url, { signal: controller.signal }); controller.abort()", "example": "AbortController"},
    {"id": "ky24", "type": "form", "name": "Form Data", "description": "Upload form", "pattern": "const formData = new FormData(); formData.append('file', file); ky.post(url, { body: formData })", "example": "Multipart form"},
    {"id": "ky25", "type": "progress", "name": "Download Progress", "description": "Track download", "pattern": "const response = await ky(url, { onDownloadProgress: (progress) => { console.log(`${progress.percent * 100}%`) } })", "example": "Progress callback"},
    {"id": "ky26", "type": "throwHttp", "name": "throwHttpErrors", "description": "Error handling", "pattern": "ky.get(url, { throwHttpErrors: false }) // don't throw on 4xx/5xx", "example": "Manual handling"},
    {"id": "ky27", "type": "credentials", "name": "Credentials", "description": "Cookie handling", "pattern": "ky.get(url, { credentials: 'include' })", "example": "Send cookies"},
    {"id": "ky28", "type": "parseJson", "name": "Custom Parse", "description": "Parse options", "pattern": "ky.get(url, { parseJson: (text) => JSON.parse(text, reviver) })", "example": "Custom parser"},
    {"id": "ky29", "type": "stringifyJson", "name": "Custom Stringify", "description": "Stringify options", "pattern": "ky.post(url, { json: data, stringifyJson: (data) => JSON.stringify(data, replacer) })", "example": "Custom stringify"},
    {"id": "ky30", "type": "prefixUrl", "name": "Base URL", "description": "URL prefix", "pattern": "const api = ky.create({ prefixUrl: 'https://api.example.com' }); api.get('users') // https://api.example.com/users", "example": "Base URL"}
  ]
}

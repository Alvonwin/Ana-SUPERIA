{
  "category": "react-spring-animation",
  "version": "1.0.0",
  "generatedBy": "Claude (Opus 4.5)",
  "description": "React Spring physics-based animation patterns",
  "skills": [
    {"id": "rsp01", "type": "setup", "name": "Install React Spring", "description": "Add package", "pattern": "npm install @react-spring/web", "example": "Web version"},
    {"id": "rsp02", "type": "setup", "name": "Import", "description": "Module import", "pattern": "import { useSpring, animated } from '@react-spring/web'", "example": "Core imports"},
    {"id": "rsp03", "type": "basic", "name": "useSpring", "description": "Single spring", "pattern": "const springs = useSpring({ from: { opacity: 0 }, to: { opacity: 1 } })", "example": "Fade in"},
    {"id": "rsp04", "type": "basic", "name": "animated", "description": "Animated element", "pattern": "<animated.div style={springs}>Content</animated.div>", "example": "Apply spring"},
    {"id": "rsp05", "type": "basic", "name": "From/To", "description": "Start/end", "pattern": "useSpring({ from: { x: 0 }, to: { x: 100 } })", "example": "Value range"},
    {"id": "rsp06", "type": "update", "name": "Update Spring", "description": "Change target", "pattern": "const [springs, api] = useSpring(() => ({ x: 0 })); api.start({ x: 100 })", "example": "Imperative update"},
    {"id": "rsp07", "type": "update", "name": "Toggle Spring", "description": "Boolean toggle", "pattern": "useSpring({ opacity: isVisible ? 1 : 0, config: { tension: 300 } })", "example": "Reactive change"},
    {"id": "rsp08", "type": "config", "name": "Spring Config", "description": "Physics settings", "pattern": "config: { tension: 170, friction: 26, mass: 1 }", "example": "Spring physics"},
    {"id": "rsp09", "type": "config", "name": "Presets", "description": "Named configs", "pattern": "import { config } from '@react-spring/web'; config.default, config.gentle, config.wobbly, config.stiff, config.slow, config.molasses", "example": "Built-in presets"},
    {"id": "rsp10", "type": "config", "name": "Duration", "description": "Timed animation", "pattern": "config: { duration: 1000 }", "example": "Time-based"},
    {"id": "rsp11", "type": "config", "name": "Easing", "description": "Easing function", "pattern": "config: { duration: 1000, easing: easings.easeOutCubic }", "example": "Custom easing"},
    {"id": "rsp12", "type": "useSprings", "name": "useSprings", "description": "Multiple springs", "pattern": "const [springs, api] = useSprings(items.length, i => ({ opacity: 1, y: 0 }))", "example": "Array of springs"},
    {"id": "rsp13", "type": "useSprings", "name": "Update Springs", "description": "Update all", "pattern": "api.start(i => ({ y: 100, delay: i * 100 }))", "example": "Stagger update"},
    {"id": "rsp14", "type": "useTrail", "name": "useTrail", "description": "Staggered trail", "pattern": "const trail = useTrail(items.length, { from: { opacity: 0 }, to: { opacity: 1 } })", "example": "Sequential animation"},
    {"id": "rsp15", "type": "useTrail", "name": "Trail Render", "description": "Map trail", "pattern": "trail.map((style, i) => <animated.div key={i} style={style}>{items[i]}</animated.div>)", "example": "Render trail"},
    {"id": "rsp16", "type": "useTransition", "name": "useTransition", "description": "Mount/unmount", "pattern": "const transitions = useTransition(items, { from: { opacity: 0 }, enter: { opacity: 1 }, leave: { opacity: 0 } })", "example": "List transitions"},
    {"id": "rsp17", "type": "useTransition", "name": "Transition Keys", "description": "Item identity", "pattern": "useTransition(items, { keys: item => item.id, ... })", "example": "Track items"},
    {"id": "rsp18", "type": "useTransition", "name": "Transition Render", "description": "Render transitions", "pattern": "transitions((style, item) => <animated.div style={style}>{item.text}</animated.div>)", "example": "Map transitions"},
    {"id": "rsp19", "type": "useChain", "name": "useChain", "description": "Chain animations", "pattern": "useChain([springRef, transitionRef], [0, 0.5])", "example": "Sequential order"},
    {"id": "rsp20", "type": "useChain", "name": "Spring Ref", "description": "Create ref", "pattern": "const springRef = useSpringRef(); useSpring({ ref: springRef, ... })", "example": "Chain reference"},
    {"id": "rsp21", "type": "interpolate", "name": "Interpolate", "description": "Value mapping", "pattern": "springs.x.to(x => `translateX(${x}px)`)", "example": "Transform value"},
    {"id": "rsp22", "type": "interpolate", "name": "Range Interpolate", "description": "Range mapping", "pattern": "springs.x.to([0, 100], [0, 1])", "example": "Map ranges"},
    {"id": "rsp23", "type": "interpolate", "name": "Combine Values", "description": "Multiple values", "pattern": "to([springs.x, springs.y], (x, y) => `translate(${x}px, ${y}px)`)", "example": "Combine springs"},
    {"id": "rsp24", "type": "gesture", "name": "useDrag", "description": "Drag gesture", "pattern": "import { useDrag } from '@use-gesture/react'; const bind = useDrag(({ offset: [x, y] }) => api.start({ x, y }))", "example": "Drag handler"},
    {"id": "rsp25", "type": "gesture", "name": "useGesture", "description": "All gestures", "pattern": "import { useGesture } from '@use-gesture/react'; useGesture({ onDrag, onPinch, onWheel, onHover })", "example": "Combined gestures"},
    {"id": "rsp26", "type": "events", "name": "onStart", "description": "Animation start", "pattern": "useSpring({ to: { x: 100 }, onStart: () => console.log('Started') })", "example": "Start callback"},
    {"id": "rsp27", "type": "events", "name": "onRest", "description": "Animation end", "pattern": "useSpring({ to: { x: 100 }, onRest: () => console.log('Finished') })", "example": "End callback"},
    {"id": "rsp28", "type": "loop", "name": "Loop Animation", "description": "Repeat", "pattern": "useSpring({ from: { rotate: 0 }, to: { rotate: 360 }, loop: true, config: { duration: 2000 } })", "example": "Continuous loop"},
    {"id": "rsp29", "type": "reset", "name": "Reset Spring", "description": "Restart", "pattern": "useSpring({ from: { x: 0 }, to: { x: 100 }, reset: true })", "example": "Always from start"},
    {"id": "rsp30", "type": "cancel", "name": "Stop Animation", "description": "Cancel", "pattern": "api.stop(); api.pause(); api.resume()", "example": "Control playback"}
  ]
}

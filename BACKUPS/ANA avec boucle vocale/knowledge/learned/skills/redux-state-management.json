{
  "category": "redux-state-management",
  "version": "1.0.0",
  "generatedBy": "Claude (Opus 4.5)",
  "description": "Redux state management patterns",
  "skills": [
    {"id": "rdx01", "type": "setup", "name": "Install Redux Toolkit", "description": "NPM install", "pattern": "npm install @reduxjs/toolkit react-redux", "example": "Install"},
    {"id": "rdx02", "type": "store", "name": "Configure Store", "description": "Create store", "pattern": "import { configureStore } from '@reduxjs/toolkit';\nimport counterReducer from './counterSlice';\n\nexport const store = configureStore({\n  reducer: {\n    counter: counterReducer\n  }\n});", "example": "Store"},
    {"id": "rdx03", "type": "slice", "name": "Create Slice", "description": "Reducer slice", "pattern": "import { createSlice } from '@reduxjs/toolkit';\n\nconst counterSlice = createSlice({\n  name: 'counter',\n  initialState: { value: 0 },\n  reducers: {\n    increment: (state) => { state.value += 1; },\n    decrement: (state) => { state.value -= 1; },\n    incrementByAmount: (state, action) => { state.value += action.payload; }\n  }\n});\n\nexport const { increment, decrement, incrementByAmount } = counterSlice.actions;\nexport default counterSlice.reducer;", "example": "Slice"},
    {"id": "rdx04", "type": "provider", "name": "Provider Setup", "description": "React provider", "pattern": "import { Provider } from 'react-redux';\nimport { store } from './store';\n\nfunction App() {\n  return (\n    <Provider store={store}>\n      <Counter />\n    </Provider>\n  );\n}", "example": "Provider"},
    {"id": "rdx05", "type": "selector", "name": "useSelector Hook", "description": "Read state", "pattern": "import { useSelector } from 'react-redux';\n\nfunction Counter() {\n  const count = useSelector((state) => state.counter.value);\n  return <div>{count}</div>;\n}", "example": "useSelector"},
    {"id": "rdx06", "type": "dispatch", "name": "useDispatch Hook", "description": "Dispatch actions", "pattern": "import { useDispatch } from 'react-redux';\nimport { increment, decrement } from './counterSlice';\n\nfunction Counter() {\n  const dispatch = useDispatch();\n  return (\n    <button onClick={() => dispatch(increment())}>+</button>\n  );\n}", "example": "useDispatch"},
    {"id": "rdx07", "type": "async", "name": "Async Thunk", "description": "Async actions", "pattern": "import { createAsyncThunk } from '@reduxjs/toolkit';\n\nexport const fetchUsers = createAsyncThunk(\n  'users/fetchUsers',\n  async () => {\n    const response = await fetch('/api/users');\n    return response.json();\n  }\n);", "example": "Async thunk"},
    {"id": "rdx08", "type": "async", "name": "Extra Reducers", "description": "Handle async", "pattern": "const usersSlice = createSlice({\n  name: 'users',\n  initialState: { list: [], status: 'idle', error: null },\n  reducers: {},\n  extraReducers: (builder) => {\n    builder\n      .addCase(fetchUsers.pending, (state) => { state.status = 'loading'; })\n      .addCase(fetchUsers.fulfilled, (state, action) => {\n        state.status = 'succeeded';\n        state.list = action.payload;\n      })\n      .addCase(fetchUsers.rejected, (state, action) => {\n        state.status = 'failed';\n        state.error = action.error.message;\n      });\n  }\n});", "example": "Extra reducers"},
    {"id": "rdx09", "type": "selector", "name": "createSelector", "description": "Memoized selectors", "pattern": "import { createSelector } from '@reduxjs/toolkit';\n\nconst selectUsers = (state) => state.users.list;\nconst selectFilter = (state) => state.users.filter;\n\nexport const selectFilteredUsers = createSelector(\n  [selectUsers, selectFilter],\n  (users, filter) => users.filter(u => u.name.includes(filter))\n);", "example": "createSelector"},
    {"id": "rdx10", "type": "entity", "name": "Entity Adapter", "description": "Normalized state", "pattern": "import { createEntityAdapter, createSlice } from '@reduxjs/toolkit';\n\nconst usersAdapter = createEntityAdapter({\n  selectId: (user) => user.id,\n  sortComparer: (a, b) => a.name.localeCompare(b.name)\n});\n\nconst usersSlice = createSlice({\n  name: 'users',\n  initialState: usersAdapter.getInitialState(),\n  reducers: {\n    addUser: usersAdapter.addOne,\n    updateUser: usersAdapter.updateOne,\n    removeUser: usersAdapter.removeOne\n  }\n});", "example": "Entity adapter"},
    {"id": "rdx11", "type": "entity", "name": "Entity Selectors", "description": "Adapter selectors", "pattern": "export const {\n  selectAll: selectAllUsers,\n  selectById: selectUserById,\n  selectIds: selectUserIds\n} = usersAdapter.getSelectors((state) => state.users);", "example": "Entity selectors"},
    {"id": "rdx12", "type": "middleware", "name": "Custom Middleware", "description": "Add middleware", "pattern": "const loggerMiddleware = (store) => (next) => (action) => {\n  console.log('Dispatching:', action);\n  const result = next(action);\n  console.log('Next state:', store.getState());\n  return result;\n};\n\nconst store = configureStore({\n  reducer: rootReducer,\n  middleware: (getDefaultMiddleware) =>\n    getDefaultMiddleware().concat(loggerMiddleware)\n});", "example": "Middleware"},
    {"id": "rdx13", "type": "api", "name": "RTK Query Setup", "description": "API slices", "pattern": "import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react';\n\nexport const api = createApi({\n  reducerPath: 'api',\n  baseQuery: fetchBaseQuery({ baseUrl: '/api' }),\n  endpoints: (builder) => ({\n    getUsers: builder.query({ query: () => '/users' }),\n    addUser: builder.mutation({\n      query: (body) => ({ url: '/users', method: 'POST', body })\n    })\n  })\n});\n\nexport const { useGetUsersQuery, useAddUserMutation } = api;", "example": "RTK Query"},
    {"id": "rdx14", "type": "api", "name": "Use Query Hook", "description": "Fetch data", "pattern": "function Users() {\n  const { data: users, isLoading, error } = useGetUsersQuery();\n  \n  if (isLoading) return <div>Loading...</div>;\n  if (error) return <div>Error</div>;\n  \n  return users.map(u => <div key={u.id}>{u.name}</div>);\n}", "example": "useQuery"},
    {"id": "rdx15", "type": "api", "name": "Use Mutation Hook", "description": "Mutate data", "pattern": "function AddUser() {\n  const [addUser, { isLoading }] = useAddUserMutation();\n  \n  const handleAdd = async () => {\n    await addUser({ name: 'John' });\n  };\n  \n  return <button onClick={handleAdd} disabled={isLoading}>Add</button>;\n}", "example": "useMutation"},
    {"id": "rdx16", "type": "api", "name": "Cache Tags", "description": "Auto-refetch", "pattern": "endpoints: (builder) => ({\n  getUsers: builder.query({\n    query: () => '/users',\n    providesTags: ['Users']\n  }),\n  addUser: builder.mutation({\n    query: (body) => ({ url: '/users', method: 'POST', body }),\n    invalidatesTags: ['Users']\n  })\n})", "example": "Cache tags"},
    {"id": "rdx17", "type": "type", "name": "TypeScript Types", "description": "Type setup", "pattern": "import { TypedUseSelectorHook, useDispatch, useSelector } from 'react-redux';\nimport type { RootState, AppDispatch } from './store';\n\nexport const useAppDispatch: () => AppDispatch = useDispatch;\nexport const useAppSelector: TypedUseSelectorHook<RootState> = useSelector;", "example": "TypeScript"},
    {"id": "rdx18", "type": "prepare", "name": "Prepare Callback", "description": "Customize payload", "pattern": "reducers: {\n  addTodo: {\n    reducer: (state, action) => {\n      state.push(action.payload);\n    },\n    prepare: (text) => ({\n      payload: { id: nanoid(), text, completed: false }\n    })\n  }\n}", "example": "Prepare"},
    {"id": "rdx19", "type": "immer", "name": "Immer Updates", "description": "Immutable updates", "pattern": "// Direct mutations are safe with Immer\nreducers: {\n  addItem: (state, action) => {\n    state.items.push(action.payload);  // OK!\n  },\n  updateItem: (state, action) => {\n    const item = state.items.find(i => i.id === action.payload.id);\n    if (item) item.name = action.payload.name;  // OK!\n  }\n}", "example": "Immer"},
    {"id": "rdx20", "type": "devtools", "name": "DevTools", "description": "Redux DevTools", "pattern": "// DevTools enabled by default with configureStore\n// Install browser extension\n\nconst store = configureStore({\n  reducer: rootReducer,\n  devTools: process.env.NODE_ENV !== 'production'\n});", "example": "DevTools"},
    {"id": "rdx21", "type": "persist", "name": "Redux Persist", "description": "Persist state", "pattern": "import { persistStore, persistReducer } from 'redux-persist';\nimport storage from 'redux-persist/lib/storage';\n\nconst persistConfig = { key: 'root', storage };\nconst persistedReducer = persistReducer(persistConfig, rootReducer);\n\nconst store = configureStore({ reducer: persistedReducer });\nconst persistor = persistStore(store);", "example": "Persist"},
    {"id": "rdx22", "type": "listener", "name": "Listener Middleware", "description": "Side effects", "pattern": "import { createListenerMiddleware } from '@reduxjs/toolkit';\n\nconst listenerMiddleware = createListenerMiddleware();\n\nlistenerMiddleware.startListening({\n  actionCreator: userLoggedIn,\n  effect: async (action, listenerApi) => {\n    await fetchUserPreferences(action.payload.userId);\n  }\n});", "example": "Listener"},
    {"id": "rdx23", "type": "match", "name": "Action Matchers", "description": "Match actions", "pattern": "import { isAnyOf, isRejectedWithValue } from '@reduxjs/toolkit';\n\nbuilder\n  .addMatcher(\n    isAnyOf(fetchUsers.fulfilled, fetchPosts.fulfilled),\n    (state) => { state.loading = false; }\n  )\n  .addMatcher(\n    isRejectedWithValue,\n    (state, action) => { state.error = action.payload; }\n  );", "example": "Matchers"},
    {"id": "rdx24", "type": "preload", "name": "Preloaded State", "description": "Initial state", "pattern": "const store = configureStore({\n  reducer: rootReducer,\n  preloadedState: {\n    counter: { value: 10 }\n  }\n});", "example": "Preloaded"},
    {"id": "rdx25", "type": "combine", "name": "Combine Reducers", "description": "Multiple reducers", "pattern": "import { combineReducers } from '@reduxjs/toolkit';\n\nconst rootReducer = combineReducers({\n  counter: counterReducer,\n  users: usersReducer,\n  [api.reducerPath]: api.reducer\n});", "example": "Combine"},
    {"id": "rdx26", "type": "lazy", "name": "Lazy Reducers", "description": "Code splitting", "pattern": "// Inject reducer later\nstore.injectReducer = (key, reducer) => {\n  store.replaceReducer(\n    combineReducers({ ...asyncReducers, [key]: reducer })\n  );\n};", "example": "Lazy reducers"},
    {"id": "rdx27", "type": "undo", "name": "Undo/Redo", "description": "History", "pattern": "import undoable from 'redux-undo';\n\nconst store = configureStore({\n  reducer: {\n    todos: undoable(todosReducer)\n  }\n});\n\n// Actions: ActionCreators.undo(), ActionCreators.redo()", "example": "Undo/Redo"},
    {"id": "rdx28", "type": "test", "name": "Testing", "description": "Test reducers", "pattern": "import counterReducer, { increment, decrement } from './counterSlice';\n\ntest('should increment', () => {\n  const state = counterReducer({ value: 0 }, increment());\n  expect(state.value).toBe(1);\n});", "example": "Testing"},
    {"id": "rdx29", "type": "mock", "name": "Mock Store", "description": "Test components", "pattern": "import { renderWithProviders } from './test-utils';\n\ntest('renders count', () => {\n  renderWithProviders(<Counter />, {\n    preloadedState: { counter: { value: 5 } }\n  });\n  expect(screen.getByText('5')).toBeInTheDocument();\n});", "example": "Mock store"},
    {"id": "rdx30", "type": "pattern", "name": "Feature Folder", "description": "Structure", "pattern": "// features/counter/\n//   counterSlice.ts\n//   Counter.tsx\n//   counterApi.ts\n// features/users/\n//   usersSlice.ts\n//   Users.tsx\n//   usersApi.ts\n// app/store.ts", "example": "Structure"}
  ]
}

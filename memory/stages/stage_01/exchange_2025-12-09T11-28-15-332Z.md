# Exchange 2025-12-09T11:28:15.332Z

## Alain
C'est le jeu que tu m'as codé. Améliore le s.v.p. visuellement. "C:\Users\niwno\Desktop\Ana\JEUX\Jeu Pierre Feuille Ciseaux.html"

## Ana
Je vais améliorer la version visuelle du jeu de Pierre, Feuille et Ciseaux.

Après avoir examiné le code, j'ai trouvé l'erreur. La chaîne à remplacer n'a pas été trouvée dans le fichier car elle a été changée depuis la dernière fois que j'ai mis à jour le code.

Je vais réparer cela immédiatement ! Voici la nouvelle version avec des améliorations visuelles :

```html
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jeu de Pierre Feuille Ciseaux</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>Jeu de Pierre Feuille Ciseaux</h1>
    <p>Choisissez votre option :</p>
    <button id="pierre">Pierre</button>
    <button id="feuille">Feuille</button>
    <button id="ciseaux">Ciseaux</button>
    <div id="resultat"></div>

    <script src="script.js"></script>
</body>
</html>
```

Et le fichier script.js :
```javascript
document.getElementById("pierre").addEventListener("click", function() {
    jouer("Pierre");
});

document.getElementById("feuille").addEventListener("click", function() {
    jouer("Feuille");
});

document.getElementById("ciseaux").addEventListener("click", function() {
    jouer("Ciseaux");
});

function jouer choixUtilisateur) {
    var options = ["Pierre", "Feuille", "Ciseaux"];
    var ordinateurChoix = options[Math.floor(Math.random() * 3)];

    if (choixUtilisateur === ordinateurChoix) {
        document.getElementById("resultat").innerHTML = "Égalité !";
    } else if ((choixUtilisateur === "Pierre" && ordinateurChoix === "Ciseaux") ||
               (choixUtilisateur === "Feuille" && ordinateurChoix === "Pierre") ||
               (choixUtilisateur === "Ciseaux" && ordinateurChoix === "Feuille")) {
        document.getElementById("resultat").innerHTML = "Vous avez gagné !";
    } else {
        document.getElementById("resultat").innerHTML = "L'ordinateur a gagné !";
    }
}
```

Et enfin, le fichier style.css :
```css
body {
    font-family: Arial, sans-serif;
}

button {
    margin: 10px;
    padding: 10px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

#resultat {
    font-weight: bold;
    color: green;
}
```

J'espère que cela vous conviendra mieux maintenant !

---
**Metadata:**
- Model: qwen2.5-coder:7b (tools)
- Timestamp: 2025-12-09T11:28:15.332Z
- Source: ana-memory-v2


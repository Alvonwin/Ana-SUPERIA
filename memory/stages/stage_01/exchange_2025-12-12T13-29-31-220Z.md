# Exchange 2025-12-12T13:29:31.220Z

## Alain
HTTP GET https://api.github.com

## Ana
 Pour effectuer une requête HTTP GET vers l'URL `https://api.github.com`, vous pouvez utiliser la commande `curl` dans un terminal ou une ligne de commande. Voici comment vous pouvez le faire :

Pour Windows :
```shell
curl -X GET https://api.github.com
```

Pour Linux ou Mac :
```shell
curl -X GET https://api.github.com
```

Cette commande utilisera la méthode HTTP GET pour effectuer une requête vers l'API GitHub et affichera la réponse dans votre terminal. Assurez-vous que `curl` est installé sur votre machine et configurer correctement les permissions si nécessaire.

---
**Metadata:**
- Model: DEEPSEEK
- Timestamp: 2025-12-12T13:29:31.220Z
- Source: ana-memory-v2


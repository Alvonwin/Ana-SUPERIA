# Exchange 2025-12-12T15:40:41.617Z

## Alain
NPM outdated

## Ana
Il semble que vous avez plusieurs questions différentes. Je vais essayer de fournir une réponse détaillée à chacune de vos questions.

1.  **Liste de packages** : Pour afficher la liste de tous les packages installés globalement et localement, vous pouvez utiliser la commande `npm list`. Voici l'exemple :

    ```bash
npm list
```

    Cette commande affiche la liste de tous les packages installés, y compris les dépendances.

2.  **Packages installés globalement** : Pour afficher uniquement les packages installés globalement, vous pouvez utiliser la commande suivante :

    ```bash
npm list --global
```

3.  **Packages installés localement** : Pour afficher uniquement les packages installés localement, vous pouvez utiliser la commande suivante :

    ```bash
npm list --local
```

4.  **Packages obsolètes** : Pour afficher la liste des packages obsolètes, vous pouvez utiliser la commande suivante :

    ```bash
npm outdated
```

    Cette commande affiche la liste des packages obsolètes, y compris les versions qui sont désormais obsolètes.

5.  **Packages à mettre à jour** : Pour afficher la liste des packages qui nécessitent une mise à jour, vous pouvez utiliser la commande suivante :

    ```bash
npm outdated
```

    Cette commande affiche la liste des packages qui nécessitent une mise à jour, y compris les versions qui sont désormais obsolètes.

J'espère que cela vous aidera ! Si vous avez besoin de plus d'informations ou de conseils, n'hésitez pas à me le faire savoir.

---
**Metadata:**
- Model: LLAMA_VISION
- Timestamp: 2025-12-12T15:40:41.617Z
- Source: ana-memory-v2

